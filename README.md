# BOT_messaging
This is an interactive BOT , takes command from user and do some actions. 
This is capable of taking inputs from user and share music url to the group.

User is allowed to command the bot for set actions, using the command handler.
For every command /request a corresponding function is called from the main.py program and actioned.
